{
  "name": "smart-project-management",
  "private": true,
  "version": "1.0.0",
  "packageManager": "pnpm@10.33.0",
  "scripts": {
    "install:deps": "pnpm install && pnpm turbo run install:deps --filter=backend",
    "dev": "turbo run dev --parallel",
    "build": "turbo run build",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "turbo": "^2.5.4"
  },
  "dependencies": {
    "tslib": "^2.8.1"
  }
}
